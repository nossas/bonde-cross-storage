{
  "name": "cross-storage-client",
  "scripts": {
    "start": "serve -s build",
    "dev": "PORT=5003 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "lint": "eslint . --fix --ext js,ts,tsx"
  }
}
